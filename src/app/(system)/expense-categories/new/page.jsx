import CreateExpenseCategory from "@/components/expense-categories/create-expense-category";

export const metadata = {
  title: "New Expense Category | EMI POS",
  description: "Create a new expense category",
};

export default function Page() {
  return <CreateExpenseCategory />;
}
