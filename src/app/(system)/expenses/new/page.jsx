import CreateExpense from "@/components/expenses/create-expense";

export const metadata = {
  title: "New Expense | EMI POS",
  description: "Record a new expense",
};

export default function Page() {
  return <CreateExpense />;
}
